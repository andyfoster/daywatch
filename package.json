{
  "name": "daywatch",
  "version": "1.0.0",
  "description": "A Chrome extension for countdown timers on new tab page",
  "main": "script.js",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest --watch",
    "validate": "node scripts/validate-project.js",
    "package:chrome": "node scripts/package-chrome-extension.js",
    "package:chrome:verify": "npm run test:run && npm run package:chrome"
  },
  "keywords": [
    "chrome-extension",
    "countdown",
    "timer",
    "new-tab"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@vitest/coverage-v8": "^2.0.5",
    "@vitest/ui": "^2.0.5",
    "happy-dom": "^15.7.4",
    "typescript": "^5.6.2",
    "vitest": "^2.0.5"
  },
  "dependencies": {
    "bootstrap": "^5.3.3"
  }
}
